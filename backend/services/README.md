# Retrieval boundary

The deployed prototype uses structured fault knowledge and prior-case context
as supporting evidence. Production Neo4j data, vector indexes, credentials,
and graph contents are not public artifacts. This placeholder documents the
boundary without shipping a database client that could be mistaken for the
complete retrieval implementation.
