# Results policy

Only independently reviewed, aggregate, simulation-scoped results should be
added to a future public release. Every result must carry a code commit,
dataset and split manifest, seed, metric definitions, failure cases, and
provenance. Raw model responses, private truth, complete telemetry, and
unreleased experiment outputs remain controlled artifacts.
